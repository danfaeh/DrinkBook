	<h1> <%= @recipe.name %> :</h1>

	<% for i in 0..(@recipe.ingredients.count-1)%>
	<h3> <%= @recipe.ingredients[i].name %></h3>
	<% end %>

	<br>
  <img id="recipe_img" src="<%= @recipe.image %>">
	<br>
	<h3> <%= @recipe.description %></h3>
	<br>
	<%= button_to "Delete", @recipe, method: :delete, data: { confirm: "Are you sure you want to remove #{@recipe.name}?" } %>
	<button onclick='window.location.href="/recipes/#{recipe.id}/edit";'>edit</button>

<br><br><br>
<br><br><br>
